OPENAI_API_KEY=sk-...
# Optional: comma-separated extra origins (regex allows *.cloudfront.net + tunnel hosts when TUNNEL_CORS_REGEX=1)
ALLOW_ORIGINS=

# Optional: set to 0 to disable automatic CORS regex for tunnel hostnames (use ALLOW_ORIGINS only)
# TUNNEL_CORS_REGEX=1
